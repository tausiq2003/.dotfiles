#!/usr/bin/env bash


#code a timer application which does this
#WIP

# if the timer is not active or not running then, left click triggers and set numbers to start running, then click on middle click to start running, if not default is 1hr
# if the timer is running left clicking on it it stops. if the timer is 0 it will close it and sends a notification timer is over. click to create a new.
#
# i don't know what to do next, but a stop watch so on first right click if the timer is not running, stop watch will be triggered and the on the right click it will stop it will echo the variable to send-notify and stop, idk what to do next.
#
#
#
#
# show_timer(){
    # while true; do
        # printf '%s\r' "$(date +%H:%M:%S:%.2N)"
    # done
# }
# 
# show_timer
